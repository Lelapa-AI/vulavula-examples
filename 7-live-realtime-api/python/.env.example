VULAVULA_API_KEY='<YOUR VALID API KEY>'
# NOTE: unlike the other examples, the Live API isn't routed on api.lelapa.ai yet -- it
# currently only exists on their-cloud-mvp's own staging host. Switch this once /v1/realtime
# is exposed on the public product domain (api.lelapa.ai / api.staging.lelapa.ai).
BASE_URL=https://triton.staging.lelapa.ai
AUDIO_FILE_PATH=<PATH TO A MONO 16-BIT PCM WAV FILE, e.g. ./data/live-audio.wav>
TARGET_LANGUAGE=
